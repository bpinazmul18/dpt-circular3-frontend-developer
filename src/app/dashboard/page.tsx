import { headers } from "next/headers";
import SecondaryHeader from "@/components/SecondaryHeader";
export default function Dashboard() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between py-2"></main>
  );
}
